
public class LevelTwo {
	
	public static void blackJack(int firstNumber, int secondNumber) {
		
		String outputMessage = "";
		
		if (firstNumber > 0 && secondNumber > 0) {
			if (firstNumber > secondNumber && firstNumber <= 21) {
				outputMessage += firstNumber; 
			}
			else if (firstNumber > secondNumber && firstNumber <= 21) {
				
			}
				
		}
		else {
			outputMessage = "Integer values must be greater than 0";
		}
		
		
		System.out.println(outputMessage);
		
	}

}
