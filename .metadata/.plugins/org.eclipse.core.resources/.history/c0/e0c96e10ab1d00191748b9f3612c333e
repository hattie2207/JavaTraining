
public class LevelTwo {
	
	static String outputMessage = "";
	
	public static String blackJack(int firstNumber, int secondNumber) {		
		
		if (firstNumber > 0 && secondNumber > 0) {
			if (firstNumber > secondNumber && firstNumber <= 21) {
				outputMessage += firstNumber; 
			}
			else if (secondNumber > firstNumber && secondNumber <= 21) {
				outputMessage += secondNumber; 
			}
			else {
				outputMessage += firstNumber; 
			}				
		}
		else {
			outputMessage = "Both integer values must be greater than 0";
		}
				
		return outputMessage;
		
	}

}
